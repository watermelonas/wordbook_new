{
	"name": "word-sync",
	"version": "1.0.0",
	"description": "单词数据同步云函数",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"uni-cloud-router": "file:../../uniCloud/uni-cloud-router"
	}
}
